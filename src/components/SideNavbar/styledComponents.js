import styled from 'styled-components'

export const NavbarContainer = styled.div`
  background-color: ${props => props.bgColor};
  min-height: 100vh;
`
export const TabContainer = styled.li`
  background-color: ${props => props.bgTabColor};
`
